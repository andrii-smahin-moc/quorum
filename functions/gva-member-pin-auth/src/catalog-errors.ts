export enum CatalogErrors {
  FAILED_CONFIG = 'Provided config(envs) is invalid',
  FAILED_GVA_PAYLOAD = 'Provided GVA payload is invalid',
  FAILED_PAYLOAD = 'Provided payload is invalid',
  GOAL_PROCESSING_ERROR = 'Error occurred during goal processing',
  REQUESTED_TYPE_NOT_SUPPORTED = 'Requested type is not supported',
}
